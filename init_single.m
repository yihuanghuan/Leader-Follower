%% init_single.m - 单机参数初始化
clear; clc;

% --- 1. 物理参数 (来自论文 Section IV) ---
m = 1; % 质量 (kg)
g = 9.81; % 重力加速度
% 转动惯量矩阵 (论文中给出的是对角阵)
J_xx = 0.04856;
J_yy = 0.04856;
J_zz = 0.08801;
J = diag([J_xx, J_yy, J_zz]); 
e3 = [0; 0; 1];

% --- 2. 控制增益 (来自论文 Section IV) ---
% 位置环参数
kp = 3; 
kv = 3; 
% 辅助变量参数 (单机时如果直接用真值，这个参数其实用不到，但为了完整性保留)
k_rho = 0.5; 
% 姿态环参数
kt = 20; 
kw = 3; 

% --- 3. 初始状态 (设为论文中 Agent 1 的初始值) ---
% 初始位置 [x; y; z]
p0 = [5; 3; -1]; 
% 初始速度
v0 = [0; 0; 0];
% 初始姿态 (四元数 [w; x; y; z], 这里设为水平)
Q0 = [1; 0; 0; 0]; 
% 初始角速度
w0 = [0; 0; 0];

% --- 4. 期望偏差 (单机跟随时的相对位置) ---
% 论文中 Agent 1 相对于领航者的偏差
delta = [1; 1; 0]; 

% --- 5. 仿真设置 ---
sim_time = 80; % 仿真时长 (秒)
dt = 0.001;    % 步长 (1ms)

disp('参数初始化完成！现在可以打开 Simulink 模型了。');